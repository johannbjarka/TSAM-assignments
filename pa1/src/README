#TFTP Server

* This TFTP server allowes downloading files from a serverside
  directory for TFTP clients. It will only accept read requests.

* It requires a portnumber to use as its only parameter  

* It naturally follows the TFTP protocol https://tools.ietf.org/html/rfc1350
